name: concurrency-13-jobset-caller-only
on: workflow_dispatch
jobs:
  caller-job:
    concurrency:
      group: caller-only-group
      cancel-in-progress: false
    uses: Bnjoroge1/aksh-conformance/.github/workflows/reusable-callee.yml@main
    with:
      val: "caller-only"
